# 이 파일을 .env.local 로 복사한 뒤 값을 채운다.
#   cp .env.example .env.local
# .env.local 은 .gitignore 대상이므로 커밋되지 않는다.

# Anthropic API 키.
# 주의: 클라이언트 번들에 노출되는 접두사(REACT_APP_ / NEXT_PUBLIC_ / VITE_)를 붙이지 말 것.
#       API 키는 서버 라우트에서만 읽는다.
ANTHROPIC_API_KEY=
